# Learn Hub

基于 VitePress 的个人教程与知识整理站，通过 GitHub Actions 自动发布到 GitHub Pages。

## 本地开发

```bash
npm install
npm run docs:dev
```

## 构建

```bash
npm run docs:build
```

## 添加内容

在 `docs/` 中新增 Markdown 文件，并在 `docs/.vitepress/config.mts` 的导航栏或侧边栏中添加入口。
